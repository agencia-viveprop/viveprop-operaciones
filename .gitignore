# Secretos / entorno
.env
.env.*
!.env.example

# Datos sensibles (nunca Excel real en el repo)
*.xlsx
*.xls
*.csv

# Python
.venv/
__pycache__/
*.pyc

# Node / frontend
node_modules/
frontend/dist/
backend/static/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Capturas temporales de verificacion (se generan al probar, no son fuente)
.tmp_screenshots/

# Imagenes de referencia sueltas en la raiz (branding, mockups). Solo la raiz:
# los assets legitimos del frontend viven en frontend/ y no se tocan.
/*.jpeg
/*.jpg

